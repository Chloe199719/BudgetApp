application:
  port: 5000

redis:
  pool_max_open: 16
  pool_max_idle: 8
  pool_timeout_seconds: 1
  pool_expire_seconds: 60

email:
  host: ${EMAIL_HOST}
  host_user: ${EMAIL_HOST_USER}
  host_user_password: ${EMAIL_HOST_USER_PASSWORD}
