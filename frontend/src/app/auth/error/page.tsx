import ErrorPage from "@/components/auth/error/ErrorPage";

type Props = {};
function page({}: Props) {
    return (
        <div>
            <ErrorPage />
        </div>
    );
}
export default page;
